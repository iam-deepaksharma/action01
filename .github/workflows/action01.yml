name: action01

on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: checkout repository
      uses: actions/checkout@v4

    - name: print date
      run: date
  job2:
   runs-on: windows-latest
   steps:
   - run: date
